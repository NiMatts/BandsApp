@model BandsApp.Web.Models.Band[]

<!DOCTYPE html>
<html>
<head>
    <title>Bands</title>
    @* <link rel="stylesheet" href="https://unpkg.com/@@picocss/pico@@latest/css/pico.classless.min.css"> *@
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body class ="min-vh-100 bg-dark text-light d-flex flex-column justify-content-between"¨>
    <div class="container mx-auto my-5">
    <div class="d-flex flex-column justify-content-center align-items-center">
        <h2>Bands</h2>
        <ul>
            @foreach (var band in Model)
            {
                 <li>
                    <a class=" text-light" href="/details/@band.Id" target="_blank">@band.Name</a>
                 </li>
            }
        </ul>
        </div>

    </div>
    <footer class="text-center p-3">&copy BandsApp Grupp8</footer>
    </body>
    </html>
